t = int(input())
out = []
for _ in range(t):
    n, d = map(int, input().split())
    s = input().strip()
    d_str = str(d)
    i = 0
    while i < len(s) and s[i] >= d_str:
        i += 1
    result = s[:i] + d_str + s[i:]
    out.append(result)
print("\n".join(out))
